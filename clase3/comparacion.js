let igualdadSimple = 10 == 15 // Igualdad → false
console.log(igualdadSimple);
let desigualdadSimple = 10 != 15 // Desigualdad → true
let igualdadEstricta = 10 === "10" // Igualdad estricta → false
let desigualdadEstricta = 10 !== 15 // Desigualdad estricta → true
let mayorQue = 15 > 15 // Mayor que → false
let mayorIgualQue = 15 >= 15 // Mayor o igual que → true
let menorQue = 10 < 15 // Menor que → true
let menorIgualQue = 10 <= 15 // Menor o igual que → true